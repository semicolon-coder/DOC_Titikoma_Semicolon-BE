<% if( alert.status !== '' || alert.message !== '' ) { %>
    <div class="h-auto col-span-12 mt-2 p-2 rounded-md shadow-md bg-<%- alert.status %>">
        <p class="text-white text-center"><%- alert.message %></p>
    </div>
<% } %>